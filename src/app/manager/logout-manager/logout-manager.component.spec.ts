/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { LogoutManagerComponent } from './logout-manager.component';

describe('LoginManagerComponent', () => {
  let component: LogoutManagerComponent;
  let fixture: ComponentFixture<LogoutManagerComponent>;

  beforeEach(async(() => {
    //TestBed.configureTestingModule({declarations: [ LoginManagerComponent ]}).compileComponents();
  }));

  beforeEach(() => {
    //fixture = TestBed.createComponent(LoginManagerComponent);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  });

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
